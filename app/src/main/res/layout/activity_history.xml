<?xml version="1.0" encoding="utf-8"?>
<androidx.constraintlayout.widget.ConstraintLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    tools:context=".view.ui.BKHistoryActivity"
    android:background="@color/history_bg">

    <TextView
        android:id="@+id/title"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        app:layout_constraintTop_toTopOf="parent"
        app:layout_constraintLeft_toLeftOf="parent"
        android:layout_marginTop="50dp"
        android:layout_marginStart="20dp"
        android:textSize="22sp"
        android:textColor="@color/white"
        android:text="Recently viewed Movies" />

    <androidx.recyclerview.widget.RecyclerView
        android:id="@+id/history_grid"
        android:layout_width="match_parent"
        android:layout_height="match_parent"
        app:layout_constraintTop_toTopOf="parent"
        app:layout_constraintLeft_toLeftOf="@id/title"
        android:layout_marginTop="100dp"
        android:layout_marginStart="20dp"/>

    <fragment
        android:id="@+id/history_synopsis"
        android:layout_width="match_parent"
        android:layout_height="match_parent"
        android:visibility="gone"
        android:name="com.kyser.demosuite.view.ui.Synopsis"/>
</androidx.constraintlayout.widget.ConstraintLayout>